# StackNLP: A language model for Stack Overflow comments

Model uses a recurrent neural network I made from scratch to generate text.

Trained on comments from Google's BigQuery Stack Overflow public dataset.
